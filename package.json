{
  "name": "@buynance/monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "./server/dist/index.js",
  "scripts": {
    "common:build": "yarn workspace @buynance/common build",
    "hardhat:test": "yarn workspace @buynance/hardhat test",
    "server:build": "yarn workspace @buynance/server build",
    "test:util": "mocha --require ts-node/register './test/helper/0_test.ts'",
    "build": "tsc -p tsconfig.server.json",
    "postbuild": "npx ts-node ./tools/build.ts",
    "start:dev": "npx ts-node --project tsconfig.json --files ./src/server/index.ts",
    "tmp": "npx ts-node --project tsconfig.server.json --files ./src/server/tmp.ts"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "server",
    "hardhat",
    "common"
  ]
}
